import { module, test } from 'qunit';
import { setupApplicationTest } from 'ember-qunit';

module('Acceptance | application role', function (hooks) {
  setupApplicationTest(hooks);

  test('the download list contains 5 downloads', async function (assert) {});

  test('a user can select a download and gets a alert with that download', async function (assert) {});

  test('a user gets a help message when selecting download without a download selected', async function (assert) {});

  test('a user gets a help message when selecting download without a download selected', async function (assert) {});

  test('a user can deselect a download', async function (assert) {});

  test('a user can select all downloads with the select all button', async function (assert) {});
});
